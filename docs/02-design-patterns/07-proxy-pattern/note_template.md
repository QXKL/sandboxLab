# 代理模式 - 学习笔记

> 为其他对象提供一个代理以控制对这个对象的访问

---

## 我的理解

### 代理模式是什么？
```
[用自己的话解释...]

提示：
- 为什么叫"代理"？
- 与生活中的门卫、经纪人有什么相似之处？
- 核心思想是什么？
```

---

## 为什么重要

代理模式解决了什么问题？

```
[在这里写...]

提示：
- 为什么需要控制访问？
- 如何优化启动速度？
- 如何实现权限控制？
```

---

## 核心要点

### 1. 代理模式的三个角色

1. Subject（抽象主题）：
2. RealSubject（真实对象）：
3. Proxy（代理）：

### 2. 三种代理类型

| 代理类型 | 目的 | 典型场景 |
|---------|------|---------|
| 虚拟代理 |  |  |
| 保护代理 |  |  |
| 缓存代理 |  |  |

### 3. 生活类比

```
[画一个门卫的示意图]

访客 → 门卫 → 住户
      (代理)
```

---

## 三种代理类型详解

### 虚拟代理（Virtual Proxy）

**目的**：


**典型场景**：


**示例代码**：
```java
[写出虚拟代理的核心代码]

class ImageProxy implements Image {
    private RealImage realImage;
    
    @Override
    public void display() {
        if (realImage == null) {
            // 懒加载
        }
    }
}
```

---

### 保护代理（Protection Proxy）

**目的**：


**典型场景**：


**示例代码**：
```java
[写出保护代理的核心代码]

class DocumentProxy implements Document {
    private User user;
    
    @Override
    public void edit() {
        if (user.hasPermission()) {
            // 权限检查
        }
    }
}
```

---

### 缓存代理（Cache Proxy）

**目的**：


**典型场景**：


**示例代码**：
```java
[写出缓存代理的核心代码]

class CacheProxy implements DataService {
    private Map<String, String> cache;
    
    @Override
    public String query(String sql) {
        if (cache.containsKey(sql)) {
            // 缓存命中
        }
    }
}
```

---

## 代码示例总结

### 示例1：VirtualProxyDemo

**场景**：


**虚拟代理解决了什么问题**：
```
[描述问题和解决方案]

问题：启动时加载所有大图片 →
解决：???
```

**性能提升**：
```
100张图片的场景：
- 直接加载：??? 秒
- 代理加载：??? 秒
- 性能提升：???
```

---

### 示例2：ProtectionProxyDemo

**场景**：


**权限矩阵**：
```
┌─────────┬──────┬──────┬──────┐
│ 角色    │ 查看 │ 编辑 │ 删除 │
├─────────┼──────┼──────┼──────┤
│ ADMIN   │      │      │      │
│ USER    │      │      │      │
│ GUEST   │      │      │      │
└─────────┴──────┴──────┴──────┘
```

**保护代理的优势**：
1. 
2. 
3. 

---

### 示例3：CacheProxyDemo

**场景**：


**缓存代理的工作原理**：
```
[描述缓存命中和未命中的流程]

查询 → 检查缓存 → ???
```

**性能对比**：
```
1000次相同查询：
- 无缓存：??? 秒
- 有缓存：??? 秒
```

---

## 代理 vs 其他模式

### vs 装饰器模式

| 对比 | 代理模式 | 装饰器模式 |
|-----|---------|-----------|
| 目的 |  |  |
| 关注点 |  |  |
| 例子 |  |  |

**如何区分**：
```
[你的判断标准...]
```

---

### vs 适配器模式

| 对比 | 代理模式 | 适配器模式 |
|-----|---------|-----------|
| 目的 |  |  |
| 接口 |  |  |
| 例子 |  |  |

---

### 三者对比表

| 模式 | 目的 | 接口 | 例子 |
|-----|------|------|------|
| 代理 |  |  |  |
| 装饰器 |  |  |  |
| 适配器 |  |  |  |

---

## 使用场景

### 何时使用代理模式？

✅ 适合的场景：
1. 
2. 
3. 

❌ 不适合的场景：
1. 
2. 
3. 

### 判断标准

```
需要控制对象访问？
  ├─ 延迟创建？ → ???
  ├─ 权限控制？ → ???
  ├─ 缓存结果？ → ???
  └─ 增强功能？ → ??? (不是代理)
```

---

## 代理模式的陷阱

### 陷阱1：混淆代理和装饰器

```
[两者有什么本质区别？]

代理：


装饰器：
```

---

### 陷阱2：过度代理

```
[多层代理有什么问题？]

问题：


解决：
```

---

### 陷阱3：代理没有控制逻辑

```
[只是简单转发算代理吗？]

错误示例：
class SimpleProxy {
    public void request() {
        realSubject.request();  // 只是转发
    }
}

正确理解：
```

---

## easySay - 用大白话讲一遍

> 假如你要给别人讲解代理模式，你会怎么说？

```
[用门卫的例子讲解...]

比如：你住在小区，访客不能直接找你，要先经过门卫...
```

---

## 代码实践

### 手写虚拟代理

```java
[不看示例，手写一个虚拟代理]

// 抽象主题
interface Image {
    void display();
}

// 真实对象
class RealImage implements Image {
    // ...
}

// 虚拟代理
class ImageProxy implements Image {
    // 懒加载
}
```

---

### 手写保护代理

```java
[手写一个权限控制代理]

// 抽象主题
interface Document {
    void view();
    void edit();
}

// 保护代理
class DocumentProxy implements Document {
    private User user;
    
    // 权限检查
}
```

---

## 真实应用

### 案例1：Spring AOP

**Spring AOP如何工作**：
```
[描述Spring AOP的代理机制]

@Before("execution(...)")
→ JDK动态代理/CGLIB
→ ???
```

**为什么是代理模式**：


---

### 案例2：Hibernate懒加载

**懒加载如何工作**：
```java
[描述Hibernate的懒加载机制]

@OneToMany(fetch = FetchType.LAZY)
private List<Order> orders;

// 访问时才加载
user.getOrders();  // 此时触发加载
```

**这是哪种代理**：


---

### 案例3：MyBatis Mapper

**Mapper接口如何工作**：
```
[描述MyBatis的代理实现]

interface UserMapper {
    User findById(int id);
}

UserMapper mapper = sqlSession.getMapper(UserMapper.class);
// 代理自动生成实现
```

---

### 案例4：你的项目

```
[描述一个你项目中可以用代理的场景]

场景：


可以用哪种代理：


如何实现：
```

---

## 常见误区

### 误区1：代理 = 装饰器

```
[为什么这是错误的？]

正确理解：
- 代理：
- 装饰器：
```

---

### 误区2：代理总是比直接访问好

```
[什么时候不需要代理？]

不需要代理的场景：
```

---

### 误区3：所有代理都要懒加载

```
[懒加载只是虚拟代理的特点]

三种代理的特点：
- 虚拟代理：
- 保护代理：
- 缓存代理：
```

---

## 自我检测

### 概念理解
- [ ] 我理解代理模式的核心思想
- [ ] 我理解三种代理类型
- [ ] 我能区分代理和装饰器
- [ ] 我理解代理模式的使用场景

### 实现能力
- [ ] 我能手写虚拟代理
- [ ] 我能手写保护代理
- [ ] 我能实现懒加载机制
- [ ] 我能实现权限控制

### 场景判断
- [ ] 我能判断何时使用代理模式
- [ ] 我能区分代理和其他模式
- [ ] 我理解Spring AOP的原理
- [ ] 我理解Hibernate懒加载

### 自测题
- [ ] 我完成了 test_01.md 的所有题目
- [ ] 我的得分 ≥ 80 分
- [ ] 我理解所有题目的答案

---

## 思考题

### 1. 为什么Hibernate要用懒加载？

```
[你的理解...]

优势：


如果不用懒加载会怎样：
```

---

### 2. 静态代理 vs 动态代理？

```
[两者有什么区别？]

静态代理：


动态代理：


Spring AOP用的是哪种：
```

---

### 3. 如何实现缓存淘汰策略？

```
[LRU、LFU等策略如何实现？]

思路：
```

---

## 核心口诀

### 记忆口诀

> **门卫控制访问权，**  
> **代理模式解难题，**  
> **懒加载权限缓存，**  
> **透明控制是关键。**

### 代理 vs 其他模式

> **代理控访问，**  
> **装饰加功能，**  
> **适配转接口，**  
> **接口要相同。**

---

## 下一步

- [ ] 完成所有代码实践（手写代理）
- [ ] 完成自测题（得分≥80分）
- [ ] 在项目中找一个可以用代理的场景
- [ ] 理解Spring AOP的实现原理
- [ ] 准备学习下一个结构型模式：**外观模式**

---

**学习日期**：____年__月__日

**💡 记住**：代理模式控制访问，不是增强功能。

**🎯 核心思想**：为其他对象提供代理以控制访问。

**⚡ 关键点**：
- 代理持有真实对象
- 代理控制何时、如何访问
- 三种类型：虚拟、保护、缓存
- 透明性：客户端无需知道
