# 适配器模式 - 学习笔记

> 让不兼容的接口能够合作

---

## 我的理解

### 适配器模式是什么？
```
[用自己的话解释...]

提示：
- 为什么叫"适配器"？
- 与生活中的插座转换器有什么相似之处？
- 核心思想是什么？
```

---

## 为什么重要

适配器模式解决了什么问题？

```
[在这里写...]

提示：
- 接口不兼容是什么意思？
- 为什么不直接修改代码？
- 如何让旧代码和新系统合作？
```

---

## 核心要点

### 1. 适配器模式的四个角色

1. Client（客户端）：
2. Target（目标接口）：
3. Adapter（适配器）：
4. Adaptee（被适配者）：

### 2. 适配器做了什么？

适配器转换的内容：
- [ ] 方法名
- [ ] 参数类型
- [ ] 参数数量
- [ ] 单位
- [ ] 返回值

### 3. 生活类比

```
[画一个插座转换器的示意图]

你的充电器 → 转换器 → 美国插座
(被适配者)  (适配器)  (目标接口)
```

---

## 对象适配器 vs 类适配器

### 对象适配器（组合）✅

```java
[写出对象适配器的标准写法]

class Adapter implements Target {
    private Adaptee adaptee;  // 组合
    
    public Adapter(Adaptee adaptee) {
        // ...
    }
    
    @Override
    public void request() {
        // ...
    }
}
```

**优点**：


**缺点**：


---

### 类适配器（继承）⚠️

```java
[写出类适配器的标准写法]

class Adapter extends Adaptee implements Target {
    @Override
    public void request() {
        // ...
    }
}
```

**优点**：


**缺点**：


---

### 对比表格

| 对比维度 | 对象适配器 | 类适配器 |
|---------|-----------|---------|
| 实现方式 |  |  |
| 灵活性 |  |  |
| 适配多个类 |  |  |
| 代码量 |  |  |
| 推荐度 |  |  |

### 我的选择

```
[你会选择哪种方式？为什么？]
```

---

## 代码示例总结

### 示例1：PaymentAdapterDemo

**场景**：


**适配前的问题**：
```
支付宝：pay(String, int cents)
微信：doPay(long, double yuan)
银联：payByUnionPay(String, String)
```

**适配后的统一接口**：
```
PaymentProcessor
  - processPayment(String orderId, double amount)
```

**适配器转换了什么**：
1. 
2. 
3. 
4. 

---

### 示例2：MediaPlayerDemo

**场景**：


**流程图**：
```
AudioPlayer
  ├─ mp3 → ???
  ├─ vlc → ???
  └─ mp4 → ???
```

**符合什么原则**：


---

### 示例3：ClassVsObjectAdapterDemo

**对象适配器的优势**：
1. 
2. 
3. 

**类适配器的限制**：
1. 
2. 
3. 

---

## 适配器模式 vs 其他模式

### vs 装饰器模式

| 对比 | 适配器模式 | 装饰器模式 |
|-----|-----------|-----------|
| 目的 |  |  |
| 接口 |  |  |
| 例子 |  |  |

**如何区分**：
```
[你的判断标准...]
```

---

### vs 代理模式

| 对比 | 适配器模式 | 代理模式 |
|-----|-----------|---------|
| 目的 |  |  |
| 接口 |  |  |
| 例子 |  |  |

---

### vs 外观模式

| 对比 | 适配器模式 | 外观模式 |
|-----|-----------|---------|
| 目的 |  |  |
| 对象数量 |  |  |
| 例子 |  |  |

---

## 使用场景

### 何时使用适配器模式？

✅ 适合的场景：
1. 
2. 
3. 

❌ 不适合的场景：
1. 
2. 
3. 

### 判断标准

```
是否需要转换接口？
  ├─ 是
  │   ├─ 能修改代码？ → 直接修改
  │   └─ 不能修改？ → ???
  └─ 否
      ├─ 增强功能？ → ???
      ├─ 简化接口？ → ???
      └─ 控制访问？ → ???
```

---

## 适配器的陷阱

### 陷阱1：过度适配

```
[什么情况下不需要适配器？]

问题：


解决：
```

---

### 陷阱2：适配器太重

```
[适配器应该包含业务逻辑吗？]

错误示例：
class HeavyAdapter {
    public void request() {
        // 100行业务逻辑...
    }
}

正确做法：
```

---

### 陷阱3：双向适配

```
[双向适配有什么问题？]

问题：


更好的方式：
```

---

## easySay - 用大白话讲一遍

> 假如你要给别人讲解适配器模式，你会怎么说？

```
[用插座转换器的例子讲解...]

比如：你去美国旅行，充电器插头和插座不匹配...
```

---

## 代码实践

### 手写对象适配器

```java
[不看示例，手写一个对象适配器]

// 目标接口
interface MediaPlayer {
    void play(String filename);
}

// 被适配者
class VLCPlayer {
    public void playVLC(String filename) { ... }
}

// 适配器
class VLCAdapter implements MediaPlayer {
    // ...
}
```

---

### 手写支付适配器

```java
[手写一个支付宝适配器]

// 目标接口
interface PaymentProcessor {
    boolean processPayment(String orderId, double amount);
}

// 被适配者：支付宝SDK
class AlipaySDK {
    public String pay(String orderId, int cents) { ... }
}

// 适配器
class AlipayAdapter implements PaymentProcessor {
    // 转换：元 → 分
    // 转换：返回值 String → boolean
}
```

---

## 真实应用

### 案例1：SLF4J日志框架

**SLF4J如何工作**：
```
[描述SLF4J如何适配不同的日志实现]

你的代码 → SLF4J接口 → ??? → Log4j/Logback
```

**为什么这么设计**：


---

### 案例2：JDBC数据库驱动

**JDBC如何工作**：
```
[描述JDBC如何适配不同数据库]

Connection conn = DriverManager.getConnection(url);
```

**不同数据库的适配器**：
- MySQL：
- PostgreSQL：
- Oracle：

---

### 案例3：InputStreamReader

**Java I/O中的适配器**：
```java
[InputStreamReader如何工作？]

InputStream in = new FileInputStream("file.txt");
Reader reader = new InputStreamReader(in, "UTF-8");
```

**适配了什么**：
- 字节流 → ???
- 接口转换：


---

### 案例4：你的项目

```
[描述一个你项目中可以用适配器的场景]

场景：


现有接口：


期望接口：


适配器设计：
```

---

## 常见误区

### 误区1：适配器 = 装饰器

```
[为什么这是错误的？]

正确理解：
- 适配器：
- 装饰器：
```

---

### 误区2：总是用类适配器

```
[为什么不推荐类适配器？]

问题：


正确做法：
```

---

### 误区3：适配器包含业务逻辑

```
[适配器应该做什么？]

应该：只做接口转换
不应该：
```

---

## 自我检测

### 概念理解
- [ ] 我理解适配器模式的核心思想
- [ ] 我理解对象适配器优于类适配器
- [ ] 我能区分适配器和装饰器
- [ ] 我理解适配器模式的使用场景

### 实现能力
- [ ] 我能手写对象适配器
- [ ] 我能识别何时需要接口转换
- [ ] 我能处理参数类型和单位转换
- [ ] 我知道适配器应该保持轻量

### 场景判断
- [ ] 我能判断何时使用适配器模式
- [ ] 我能区分适配器和其他模式
- [ ] 我理解系统集成场景
- [ ] 我知道何时不需要适配器

### 自测题
- [ ] 我完成了 test_01.md 的所有题目
- [ ] 我的得分 ≥ 80 分
- [ ] 我理解所有题目的答案

---

## 思考题

### 1. 为什么推荐组合而不是继承？

```
[你的理解...]

组合的优势：


继承的限制：
```

---

### 2. 适配器可以适配多个类吗？

```
[你的想法...]

场景：


实现方式：
```

---

### 3. 接口差异太大怎么办？

```
[如果转换逻辑非常复杂？]

判断标准：


解决方案：
```

---

## 核心口诀

### 记忆口诀

> **接口不兼容，**  
> **适配器来帮忙，**  
> **组合优于继承，**  
> **转换要轻量。**

### 适配器 vs 装饰器

> **适配器改接口，**  
> **装饰器加功能，**  
> **接口不相同，**  
> **装饰器相同。**

---

## 下一步

- [ ] 完成所有代码实践（手写适配器）
- [ ] 完成自测题（得分≥80分）
- [ ] 在项目中找一个可以用适配器的场景
- [ ] 理解SLF4J、JDBC等真实案例
- [ ] 准备学习下一个结构型模式：**装饰器模式**

---

**学习日期**：____年__月__日

**💡 记住**：适配器模式不改变功能，只转换接口。

**🎯 核心思想**：让不兼容的接口能够合作。

**⚡ 关键点**：
- 对象适配器（组合）优于类适配器（继承）
- 适配器应该轻量，只做接口转换
- 区分适配器、装饰器、代理、外观模式
